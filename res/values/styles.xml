<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <style name="NoBiaoti" parent="android:Theme.Holo.Light.NoActionBar"></style>
    
    <!-- 所有标题栏的样式 -->
    <style name="TitlebarStyle" parent="android:Theme.Light">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">false</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:windowTitleSize">@dimen/comm_titlebar_height</item>
        <item name="android:windowTitleBackgroundStyle">@style/TitlebarBackgroud</item>
    </style>
    
    <style name="MyStyle01" parent="android:Theme.Light">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">false</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:windowTitleSize">@dimen/comm_titlebar_height</item>
        <item name="android:windowTitleBackgroundStyle">@style/TitlebarBackgroud</item>
        <item name="android:windowAnimationStyle">@style/AnimationActivity01</item>
    </style>
    
    <style name="AnimationActivity01" mce_bogus="1" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/push_left_in</item>
        <item name="android:activityOpenExitAnimation">@anim/push_left_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/push_left_in</item>
        <item name="android:activityCloseExitAnimation">@anim/push_left_out</item>
    </style>
    
    <style name="TitlebarBackgroud">
        <item name="android:background">@color/titlebar_bg</item>
    </style>

</resources>
